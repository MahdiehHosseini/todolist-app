// import pakages
import { createSlice, PayloadAction } from '@reduxjs/toolkit'
// import types & interfaces

const initialState = {}
const subReducersSlice = createSlice({
	name: 'handleGoalsData',
	initialState,
	reducers: {
		
	}
})

export const {  } = subReducersSlice.actions
export default subReducersSlice.reducer
